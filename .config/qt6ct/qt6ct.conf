# qt6ct pre-seed for the App Theming Bridge (ADR 0102/0124). Points qt6ct at
# Noctalia's KColorScheme file (kcolorscheme template, ADR 0123) — a `.colors`
# file, NOT the qt6ct QPalette `colors/noctalia.conf`. qt6ct-kde detects it
# (Qt6CT::isKColorScheme) and applies the FULL KColorScheme via
# KColorScheme::createApplicationPalette, so KColorScheme apps (Dolphin/Gwenview/
# Kate) follow the palette's accent roles (selection, focus) too — not just the
# base QPalette that the .conf gave (ADR 0124). qt6ct expands ~, so this is
# portable across users / skel-seeding. Noctalia writes the .colors itself.
# style=Fusion so Qt honours the custom palette; icon_theme matches GTK's.
[Appearance]
color_scheme_path=~/.local/share/color-schemes/noctalia.colors
custom_palette=true
icon_theme=Papirus-Dark
style=Fusion
