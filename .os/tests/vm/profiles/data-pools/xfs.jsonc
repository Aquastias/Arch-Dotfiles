// VM test: per-group xfs Standalone Data Pool (ADR 0043). 2 × 20 GiB —
//   /dev/sda → rpool (OS, zfs, topology=none, single disk)
//   /dev/sdb → tank0 (Standalone Data Pool, filesystem=xfs, /data/tank0)
// The xfs disk is dispatched to the xfs Data Group Formatter (mkfs.xfs + fstab),
// NOT a zpool. verify asserts the zfs root imported AND the xfs data disk is
// mounted at boot (findmnt — it has no dataset to query).
{
  "name": "arch-zfs-test-data-pools-xfs",
  "hardware": { "disks": [20, 20], "ram_mb": 4096, "vcpus": 2 },
  "install": {
    "system": {
      "hostname": "vm-test-data-pools-xfs",
      "locale":   "en_US.UTF-8",
      "timezone": "UTC",
      "keymap":   "us"
    },
    "options": {
      "kernel":      "lts",
      "bootloader":  "systemd-boot",
      "encryption":  false,
      "swap":        true,
      "swap_size":   "auto"
    },
    "packages": { "extra": [], "groups": {} },
    "post_install": {
      "backup": false,
      "security": false,
      "desktop": { "kde": false }
    },
    "mode": "multi",
    "os_pool": {
      "pool_name": "rpool",
      "topology":  "none",
      "ashift":    12,
      "disks":     ["/dev/sda"]
    },
    "storage_groups": [],
    "data_pools": [
      { "name": "tank0", "filesystem": "xfs", "disks": ["/dev/sdb"] }
    ]
  },
  "verify": {
    "boot":      true,
    "pools":     ["rpool"],
    "fs_mounts": ["/data/tank0"]
  }
}
