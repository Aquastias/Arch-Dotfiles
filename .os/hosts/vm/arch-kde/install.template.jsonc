// Install Template for vm/arch-kde — overrides hosts/core/install.template.jsonc.
// Per-machine fields only; layout (mode, disk) and hostname come from the picker.

{
  "environment": {
    "desktop": "kde",
    "gpu":     "auto"
  },

  "ashift": 13
}
