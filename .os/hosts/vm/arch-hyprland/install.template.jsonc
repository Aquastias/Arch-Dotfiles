// Install Template for vm/arch-hyprland — overrides hosts/core/install.template.jsonc.
// Per-machine fields only; layout (mode, disk) and hostname come from the picker.

{
  "environment": {
    "desktop": "hyprland",
    "gpu":     "auto"
  },

  "ashift": 13
}
