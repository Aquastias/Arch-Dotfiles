// Host Config for vm/arch-secure — VM smoke-test host that exercises
// SOPS + impermanence + ZFS native encryption together.
//
// Merged on top of `.os/hosts/core/config.jsonc`. The Host Core already
// pulls in the `sops` system program; no other system programs are needed
// for the smoke test to demonstrate the combined install path.

{
  "users": ["vm-test"],
  "system_programs": []
}
