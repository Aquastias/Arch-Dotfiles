// Install Template for the `laptop` Host Profile.
// Merged on top of .os/hosts/core/install.template.jsonc by tools/pick.sh.
// Setting system.hostname pins this profile's machine identity; the
// picker writes that value into install.jsonc instead of falling back
// to the profile name.

{
  "system": {
    "hostname": "chronos"
  }
}
