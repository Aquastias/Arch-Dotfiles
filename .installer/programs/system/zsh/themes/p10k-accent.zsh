# SEED — resolved copy of templates/p10k-accent.zsh for the fleet default
# palette (Catppuccin Mocha Sapphire, ADR 0109). install.sh copies this to
# ~/.zsh/themes/p10k-accent.zsh and /etc/skel. Noctalia overwrites it live in
# compositor sessions. Keep in sync with templates/p10k-accent.zsh.

typeset -g POWERLEVEL9K_DIR_FOREGROUND='#74c7ec'
typeset -g POWERLEVEL9K_DIR_ANCHOR_FOREGROUND='#74c7ec'
typeset -g POWERLEVEL9K_DIR_SHORTENED_FOREGROUND='#a6adc8'
typeset -g POWERLEVEL9K_OS_ICON_FOREGROUND='#74c7ec'
