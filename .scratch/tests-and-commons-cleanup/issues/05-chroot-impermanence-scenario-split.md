Status: wontfix

# Scenario-split chroot-impermanence.bats (conditional)

## Parent

`.scratch/tests-and-commons-cleanup/PRD.md`

## What to build

Conditional on the post-issue-01 measurement. Today
`chroot-impermanence.bats` runs 73 tests, each of which re-runs
`impermanence_apply` end-to-end (~420ms/test, 30.6s sequential —
54% of the pre-parallelism baseline). After issue 01 lands
`bats --jobs "$(nproc)"`, the file should drop significantly on a
24-core host, but if it still dominates the parallel wall time, the
wasted work is still wasted.

**Gate**: only act on this issue if the post-issue-01 measurement
shows `chroot-impermanence.bats` taking more than ~5s under parallel
execution. The measurement is recorded as a comment on issue 01.

If acting: split the 73 tests into ~4–6 `.bats` files by scenario
(disabled, enabled-default, enabled+persist-dirs,
enabled+persist-files, etc.). Each file runs `impermanence_apply`
once in `setup_file()` and all `@test` cases in that file assert
against the shared `$CALLS` log set up by the file-level setup.
Keeps fine-grained test names; collapses ~73 applies to ~5.

## Acceptance criteria

- [ ] Measurement from issue 01 reviewed; decision (proceed /
      defer) recorded as a comment on this issue
- [ ] If proceeding: original `chroot-impermanence.bats` split into
      one file per scenario, each using `setup_file()` to run
      `impermanence_apply` exactly once
- [ ] Per-test `@test` names preserved as much as possible — no
      consolidation into "fat" multi-assertion tests
- [ ] All assertions still run against the same observable surface
      (`$CALLS` log + generated files under `$FAKEROOT`)
- [ ] Full bats suite still passes; total wall time decreases or
      stays flat
- [ ] If deferring: close this issue with the measurement noted

## Blocked by

`.scratch/tests-and-commons-cleanup/issues/01-bats-parallelism.md`

## Comments

> *This was generated by AI during triage.*

### Triage Notes

Maintainer override: moved from `ready-for-human` to
`ready-for-agent`. Agent owns the full flow — read the measurement
from issue 01, apply the ~5s threshold, then either close (defer)
or perform the scenario split. The threshold is a soft cutoff; if
the number is borderline, err on the side of closing (defer) and
let the maintainer reopen if needed.

### 2026-05-24 — deferred per gate

Measurement from issue 01 (post-parallelism, 24-core host):
`chroot-impermanence.bats` runs in **4.83s** parallel wall time.
Threshold is ~5s; per triage note "if borderline, err on the side
of closing (defer)". Closing as `wontfix`. Maintainer can reopen
if the file grows past the threshold.
