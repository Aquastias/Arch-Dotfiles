# Hyprland Desktop Environment Adapter

Status: done
Category: enhancement

## Parent

`.scratch/environment-selection/PRD.md`

## What to build

Create a new Hyprland Desktop Environment Adapter following the same pattern as the KDE adapter: `extras/desktop/hyprland/hyprland.sh` and `extras/desktop/hyprland/install-hyprland.jsonc`.

`hyprland.sh` installs `hyprland` unconditionally, then reads `install-hyprland.jsonc` to install toggled companion packages. Default companion set (all true by default):

| Toggle key | Package |
|---|---|
| `bar` | `waybar` |
| `notifications` | `dunst` |
| `launcher` | `fuzzel` |
| `rofi` | `rofi-wayland` |
| `terminal` | `alacritty` |
| `lock` | `hyprlock` |
| `idle` | `hypridle` |
| `wallpaper` | `hyprpaper` |

Always installs `xdg-desktop-portal-hyprland` and `polkit-kde-agent` regardless of toggles.

Display manager selection: reads `ENVIRONMENT_DESKTOP` env var. If `"kde"` is absent from the array, installs `greetd` + `greetd-tuigreet`, writes `/etc/greetd/config.toml` pointing tuigreet at the Hyprland session, and enables greetd. If `"kde"` is present, skips all greeter steps — SDDM (installed by the KDE adapter) handles the display manager and Hyprland appears as a session.

Reference for companion package selection: stepskop/.dotfiles on GitHub.

## Acceptance criteria

- [ ] `"desktop": "hyprland"` installs Hyprland, all default companions, greetd+tuigreet; greetd service is enabled; `/etc/greetd/config.toml` is written with `tuigreet --cmd Hyprland`
- [ ] `"desktop": ["kde", "hyprland"]` installs Hyprland and companions; greetd is NOT installed; SDDM (from KDE adapter) is the active display manager
- [ ] Setting a companion toggle to `false` in `install-hyprland.jsonc` results in that package not being passed to pacman
- [ ] `xdg-desktop-portal-hyprland` and `polkit-kde-agent` are always installed regardless of toggles
- [ ] `install-hyprland.jsonc` has all companions defaulting to `true` with inline comments naming the package each toggle controls
- [ ] BATS tests cover: Hyprland-only (greetd installed), KDE+Hyprland (greetd skipped), companion toggle respected (disabled companion absent from pacman call), always-installed packages present regardless of toggles
- [ ] Shellcheck passes on all new scripts

## Blocked by

- `issues/04-environment-runner-kde-adapter.md`

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Create Hyprland Desktop Environment Adapter with companion toggles and conditional display manager selection.

**Current behavior:**
No Hyprland support exists. The extras directory has only a KDE adapter. Setting `"desktop": "hyprland"` in the Install Config (once issue #04 lands) will produce a "script not found" error from the Environment Runner.

**Desired behavior:**
A new Hyprland Desktop Environment Adapter at `extras/desktop/hyprland/hyprland.sh` with companion config at `extras/desktop/hyprland/install-hyprland.jsonc` — same pattern as the KDE adapter.

`hyprland.sh` always installs `hyprland`, `xdg-desktop-portal-hyprland`, and `polkit-kde-agent`. It reads `install-hyprland.jsonc` and installs enabled companion packages via pacman. It checks `ENVIRONMENT_DESKTOP` to decide the display manager:

- `"kde"` absent → install `greetd` + `greetd-tuigreet`, write `/etc/greetd/config.toml`, enable greetd service
- `"kde"` present → skip all DM steps (SDDM from KDE adapter handles it; Hyprland appears as a session automatically)

`install-hyprland.jsonc` companion toggles (all default `true`):

| Key | Package |
|---|---|
| `bar` | `waybar` |
| `notifications` | `dunst` |
| `launcher` | `fuzzel` |
| `rofi` | `rofi-wayland` |
| `terminal` | `alacritty` |
| `lock` | `hyprlock` |
| `idle` | `hypridle` |
| `wallpaper` | `hyprpaper` |

`/etc/greetd/config.toml` content (Hyprland-only path):
```toml
[terminal]
vt = 1

[default_session]
command = "tuigreet --cmd Hyprland"
user = "greeter"
```

Reference implementation for companion selection: stepskop/.dotfiles on GitHub.

**Key interfaces:**
- `extras/desktop/hyprland/hyprland.sh` — reads `install-hyprland.jsonc` via the same `jsonc()` helper used by `kde.sh`; reads `ENVIRONMENT_DESKTOP` for DM selection; calls `pacman -S --noconfirm --needed`; calls `systemctl enable greetd` or skips.
- `extras/desktop/hyprland/install-hyprland.jsonc` — eight boolean toggle keys with inline comments naming the package each controls.
- `/etc/greetd/config.toml` — written by the adapter inside the chroot when greetd path is taken.
- BATS tests — mock `pacman`, `systemctl`, and file writes. Cover: Hyprland-only (greetd installed + config written + service enabled), KDE+Hyprland (greetd steps skipped), disabled companion absent from pacman args, always-installed packages present regardless of toggles.

**Acceptance criteria:**
- [ ] `ENVIRONMENT_DESKTOP="hyprland"` → greetd+greetd-tuigreet installed, `/etc/greetd/config.toml` written, greetd enabled
- [ ] `ENVIRONMENT_DESKTOP="kde hyprland"` → greetd NOT installed, no config written, greetd NOT enabled
- [ ] Setting any companion toggle to `false` → that package absent from pacman call
- [ ] `xdg-desktop-portal-hyprland` and `polkit-kde-agent` always installed
- [ ] `install-hyprland.jsonc` has all 8 companion toggles defaulting to `true` with package name in comment
- [ ] BATS tests pass for all cases above
- [ ] Shellcheck passes on all new scripts

**Out of scope:**
- Hyprland dotfiles / config file deployment (no `.config/hypr/` written by the adapter)
- Any display manager other than greetd+tuigreet for the Hyprland-only case
- Changes to KDE adapter or Environment Runner (issue #04)
- GPU or audio resolution
