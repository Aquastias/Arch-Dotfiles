# Single entry-point install.sh

Status: done
Category: enhancement

## Parent

`.scratch/declarative-install-config/PRD.md`

## What to build

Build `.os/install.sh` as the documented entry point for the entire flow. It calls the existing numbered scripts in sequence: `01-bootstrap-zfs.sh` → `02-wipe.sh` → `03-install.sh`. Disks are always wiped — the wipe step is no longer optional.

After the install completes, clean up any temporary scripts or files created during the run (`profiles.sh` may produce intermediate state; nothing should remain in the new system or in `/mnt`).

Update `.os/README.md` and `.os/REFERENCE.md` so the documented quick-start runs `./install.sh` instead of the three separate scripts.

## Acceptance criteria

- [ ] `.os/install.sh` exists and is executable
- [ ] It runs bootstrap, wipe, and main install in order, aborting on any failure
- [ ] `01-bootstrap-zfs.sh`, `02-wipe.sh`, `03-install.sh` are still on disk and still individually runnable for debugging
- [ ] No temporary scripts remain in the installed system or `/mnt` after a successful install
- [ ] `.os/README.md` quick-start documents `./install.sh` as the entry point
- [ ] `.os/REFERENCE.md` updated to match
- [ ] VM install verified end-to-end through the single entry point

## Blocked by

- `.scratch/declarative-install-config/issues/04-profile-runner-user-creation.md`

## Comments

### Triage — ready-for-agent

> *This was generated by AI during triage.*

**Category:** enhancement
**State:** ready-for-agent

Design is fully specified by the parent PRD and the four ADRs in `docs/adr/`. The "What to build" and "Acceptance criteria" sections of this issue serve as the agent brief. Blockers are explicit; an agent should not start until they are merged. VM verification is required for issues that touch the install flow (04, 05, 06, 07).

**Reference material:**
- Parent PRD: `.scratch/declarative-install-config/PRD.md`
- Glossary: `CONTEXT.md`
- ADRs: `docs/adr/0001..0004`
