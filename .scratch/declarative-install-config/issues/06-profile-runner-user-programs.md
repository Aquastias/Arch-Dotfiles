# Profile runner — user program install + paru bootstrap

Status: ready-for-agent
Category: enhancement

## Parent

`.scratch/declarative-install-config/PRD.md`

## What to build

Extend `.os/lib/profiles.sh` to install user programs declared in each merged user config. For every user, the runner first bootstraps paru inside the chroot — building it as the user from AUR via `arch-chroot /mnt su - <username>` — then installs each user-level program via the same mechanism. User-level programs are those marked `system: false` in their `config.jsonc`.

Validation aborts the install if any user config references a program marked `system: true`.

Migrate `teamspeak3` from `.pkglist/programs/communication/teamspeak3/` into `.os/programs/communication/teamspeak3/` as the user-level proof program, including its `addons/` directory. The original under `.pkglist/` is left untouched.

End-to-end demo: VM install lands with users created, firewalld installed at system level, paru built per user, and teamspeak3 installed under each user's home — all before first boot.

## Acceptance criteria

- [ ] profiles.sh bootstraps paru per user via `arch-chroot /mnt su - <username>` before any user programs are installed
- [ ] User programs are installed via `arch-chroot /mnt su - <username>` running paru
- [ ] Validation aborts the install if a user config references a program with `system: true`
- [ ] Validation aborts the install if a user config references a non-existent program
- [ ] `.os/programs/communication/teamspeak3/config.jsonc` exists with `system: false`
- [ ] `.os/programs/communication/teamspeak3/install.sh` installs the package and copies the bundled icons + theme
- [ ] `.os/programs/communication/teamspeak3/addons/` contains the bundled icons and theme files
- [ ] `.pkglist/programs/communication/teamspeak3/` is unchanged
- [ ] No first-boot scripts are dropped into user homes
- [ ] VM install verified: first login as the user reveals teamspeak3 installed with icons + theme already in place

## Blocked by

- `.scratch/declarative-install-config/issues/05-profile-runner-system-programs.md`

## Comments

### Triage — ready-for-agent

> *This was generated by AI during triage.*

**Category:** enhancement
**State:** ready-for-agent

Design is fully specified by the parent PRD and the four ADRs in `docs/adr/`. The "What to build" and "Acceptance criteria" sections of this issue serve as the agent brief. Blockers are explicit; an agent should not start until they are merged. VM verification is required for issues that touch the install flow (04, 05, 06, 07).

**Reference material:**
- Parent PRD: `.scratch/declarative-install-config/PRD.md`
- Glossary: `CONTEXT.md`
- ADRs: `docs/adr/0001..0004`
