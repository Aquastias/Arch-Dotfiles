# Shellcheck baseline across .os/

Status: ready-for-agent
Category: enhancement

## Parent

`.scratch/declarative-install-config/PRD.md`

## What to build

Run shellcheck across every shell script under `.os/` and fix all findings. Add a wrapper or doc snippet that lets the maintainer re-run the check before committing. This is the primary safety net for shell pitfalls that aren't worth writing dedicated tests for.

## Acceptance criteria

- [x] `shellcheck` runs cleanly on every `*.sh` file under `.os/`
- [x] A documented way to re-run the check exists (script under `.os/tests/`, or a one-liner in REFERENCE.md)
- [x] Any shellcheck disables added are accompanied by a one-line comment justifying the disable
- [x] `.pkglist/` is not modified

## Blocked by

None - can start immediately

## Comments

### Triage — ready-for-agent

> *This was generated by AI during triage.*

**Category:** enhancement
**State:** ready-for-agent

Design is fully specified by the parent PRD and the four ADRs in `docs/adr/`. The "What to build" and "Acceptance criteria" sections of this issue serve as the agent brief. Blockers are explicit; an agent should not start until they are merged. VM verification is required for issues that touch the install flow (04, 05, 06, 07).

**Reference material:**
- Parent PRD: `.scratch/declarative-install-config/PRD.md`
- Glossary: `CONTEXT.md`
- ADRs: `docs/adr/0001..0004`
