# Migrate virtualization category into .os/programs/

Status: ready-for-agent
Category: enhancement

## Parent

`.scratch/declarative-install-config/PRD.md`

## What to build

Port the `virtualization/` programs from `.pkglist/programs/virtualization/` into `.os/programs/virtualization/`: `docker/` and `virt-manager/`. Each program ships with `config.jsonc` (metadata) and `install.sh` (source of truth). The `.pkglist/` originals are left untouched.

## Acceptance criteria

- [ ] `.os/programs/virtualization/docker/config.jsonc` exists with `system: true`
- [ ] `.os/programs/virtualization/docker/install.sh` installs Docker, enables the daemon, and adds appropriate group membership
- [ ] `.os/programs/virtualization/virt-manager/config.jsonc` exists with `system: true`
- [ ] `.os/programs/virtualization/virt-manager/install.sh` installs virt-manager + KVM/QEMU stack and enables required services
- [ ] All install.sh scripts source `.os/lib/shell-stdlib.sh` and use the standard env vars
- [ ] `.pkglist/programs/virtualization/` is unchanged
- [ ] Each program installs successfully on a VM run when referenced from a config

## Blocked by

- `.scratch/declarative-install-config/issues/05-profile-runner-system-programs.md`

## Comments

### Triage — ready-for-agent

> *This was generated by AI during triage.*

**Category:** enhancement
**State:** ready-for-agent

Design is fully specified by the parent PRD and the four ADRs in `docs/adr/`. The "What to build" and "Acceptance criteria" sections of this issue serve as the agent brief. Blockers are explicit; an agent should not start until they are merged. VM verification is required for issues that touch the install flow (04, 05, 06, 07).

**Reference material:**
- Parent PRD: `.scratch/declarative-install-config/PRD.md`
- Glossary: `CONTEXT.md`
- ADRs: `docs/adr/0001..0004`
