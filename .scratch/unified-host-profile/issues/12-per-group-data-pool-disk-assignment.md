# Per-group data-pool disk assignment (picker + VM harness)

Status: done

## Parent

`.scratch/unified-host-profile/PRD.md`

## What to build

Close the deferred follow-up that blocks installing any multi-data-pool
host (e.g. `arch-data`) via `--profile`. Today both front-ends assign
*all* picked disks to `os_pool` and never populate `storage_groups[]` /
`data_pools[]`, so a host that declares data pools can't assemble (the
groups get 0 disks → validation fails). Surfaced verifying issue 08.

Two coupled problems:

1. **Distribution.** Neither `install.sh:_install_pick_assignment` (the
   interactive picker) nor `vm/lib/profile.sh:_profile_resolve_host` (the
   VM harness) maps picked disks onto the declared
   `os_pool` + `storage_groups[]` + `data_pools[]` groups. They emit only
   `{mode:"multi", os_pool:[...all disks...]}`. The downstream
   `picker_assign_disks` already *accepts* a per-group assignment
   (`{os_pool:[...], storage_groups:[[...]], data_pools:[[...]]}`) and
   validates each — so the gap is purely on the producing side: decide how
   many disks each group gets.

2. **Validator min-disk conflict.** `picker_assign_disks` validates each
   group via `picker_validate_layout`, whose table is `none`/`stripe` **>=2**
   (`lib/picker.sh`). But the actual layout, `layout_validate`
   (`lib/layout/multi.sh`), treats `none` as a **1-disk** OS pool and
   `stripe` as a **1-disk** "independent" alias — which is exactly what
   `arch-data` (os_pool `none`, `tank0` `stripe`) relies on. So even a
   correct per-group assignment is rejected by `picker_validate_layout`.
   These two validators must be reconciled (or the assignment path must
   validate against the layout semantics, not the picker-prompt semantics)
   without regressing the interactive picker's own min-disk tests
   (`tests/picker.bats`, `tests/picker-assign.bats`).

## Acceptance criteria

- [x] `install.sh --profile <multi-data-pool-host>` maps picked disks onto
      every declared group (os_pool + storage_groups + data_pools),
      validated against the layout's real min-disk rules.
- [x] `vm/lib/profile.sh:_profile_resolve_host` does the same for a VM disk
      list, so `host_profile: arch-data` assembles + installs.
- [x] `none` (1-disk OS pool) and `stripe`/independent (1-disk data pool)
      are accepted on the assignment path; `mirror`>=2 / `raidz1`>=3 /
      `raidz2`>=4 still enforced and named per group on failure.
- [x] Interactive-picker min-disk behaviour + its bats suite stay green
      (no regression from the reconciliation).
- [x] `arch-data` installs end-to-end via a `host_profile` VM run with
      `vm-data` owning `tank0`/`tank1` — closes issue 08 AC3.

## Design (resolved — ADR 0037)

Both open questions are decided in `docs/adr/0037-per-group-disk-count-
assignment.md`:

- **Disk count** is declared per group: `os_pool.disk_count`,
  `storage_groups[].disk_count`, `data_pools[].disk_count` (integer;
  `os_pool` `none`/`single` ⇒ 1). The profile keeps no `disks`; the
  install populates `disks` of length `disk_count`. A group's shape is
  `topology` + `disk_count`, read the same everywhere.
- **Assignment**: operator multi-selects exactly `sum(disk_count)` disks;
  picker + harness assign them to groups **in declared order** (os_pool →
  storage_groups[] → data_pools[]); the picker review screen shows the
  per-group mapping. Wrong total aborts naming the expected count.
- **Validation**: the assignment path gets its OWN pool-topology min check
  (`stripe`≥1, `mirror`≥2, `raidz1`≥3, `raidz2`≥4; `os_pool none`/`single`
  =1), aligned with `layout_validate` / Standalone-Data-Pool rules.
  `picker_validate_layout` (the interactive OS-pool *mode* prompt) is left
  untouched — it validates a different thing (mode + leftover-folding).

## Blocked by

- None (the loader/assembler seam `picker_assign_disks` already exists).

## Relates to

- `.scratch/unified-host-profile/issues/03-install-profile-frontend.md`
  (deferred this as the per-group follow-up).
- `.scratch/unified-host-profile/issues/08-migration-tracer-arch-data.md`
  (AC3 blocked on this).
- `docs/adr/0037-per-group-disk-count-assignment.md` (decision of record).

## Comments

### Agent brief

> *This was generated by AI during triage.*

Decision of record: **ADR 0037**. Grilled + confirmed with the operator.

**Touch points (read these first):**
- `lib/picker.sh` — `picker_assign_disks` (multi branch) already merges a
  per-group `assignment` into the skeleton, but validates every group via
  `picker_validate_layout` (the OS-mode prompt rules). Add a per-group
  pool-topology min check (`stripe`≥1, `mirror`≥2, `raidz1`≥3, `raidz2`≥4;
  `os_pool none`/`single`=1) and use it for `storage_groups[]`/`data_pools[]`
  (and `os_pool` in the profile context). Do NOT change
  `picker_validate_layout` — it stays the interactive OS-mode validator.
- `vm/lib/profile.sh` — `_profile_resolve_host` (multi case, ~line 88)
  currently builds `{mode:multi, os_pool:[all disks]}`. Build the full
  assignment `{os_pool:[…], storage_groups:[[…],…], data_pools:[[…],…]}`
  by slicing the VM's `profile_disk_devices` list per `disk_count` in
  declared order; abort if VM disk count != `sum(disk_count)`.
- `install.sh` — `_install_pick_assignment` (only resolves `os_pool`
  today; its own comment flags this as the follow-up). Multi-select
  `sum(disk_count)` disks, slice per `disk_count` in declared order into
  the assignment, and render the per-group mapping on the review screen.
- `lib/config/profile.sh` — add `os_pool.disk_count`,
  `storage_groups[].disk_count`, `data_pools[].disk_count` to
  `_PROFILE_SCHEMA_host`. Accessors in `lib/config/accessors.sh` if the
  assignment reads via the effective config (it reads the profile JSON).
- **Data**: add `disk_count` to every multi host profile — `arch-data`
  (os_pool none 1, tank0 stripe 1, tank1 mirror 2), `desktop` (os_pool
  mirror 2, storage_group `data` raidz1 3), `arch-secure` (os_pool mirror
  2). Single-disk hosts: 1. Confirm each `host_profile`-referencing VM
  profile's `hardware.disks` count == that host's `sum(disk_count)`.

**The layout module is unchanged** — `resolve_data_pools` /
`resolve_storage_topologies` already read `*.disks` from the effective
config; this issue only populates those arrays upstream.

**Tests (bats, behaviour-level):** distribution (given a skeleton +
disk_counts + a picked-disk list, assert the per-group assignment and the
declared-order slicing); the new per-group min check (raidz1 with 2 →
fail naming the group; stripe with 1 → ok; os_pool none with 1 → ok);
wrong-total abort. Extend `tests/picker-assign.bats`,
`tests/config/profile-loader.bats`; the VM equivalence path in
`tests/vm/profile.bats`. Add the `disk_count` keys to fixtures.

**Verify (VM, the real prize):** `vm.sh --testing --verify-boot --profile
<a host_profile-arch-data profile>` (add one) installs arch-data via the
real profile with `vm-data` owning `tank0`/`tank1` — closing issue 08 AC3.

**Coordinate with issue 11:** the `disk_count` field belongs in the
CONTEXT.md rewrite (Storage Group / Standalone Data Pool / os_pool terms).
Left for issue 11 to avoid touching the stale legacy glossary piecemeal.

### Agent resolution (Claude) — 2026-06-11

Implemented per ADR 0037, TDD (RED→GREEN per slice). Non-VM suite green
(252 ok across the affected + adjacent suites; 0 fail). `shellcheck -x`
clean on the touched `.sh` (only benign SC1091 source-following info).

**Shipped:**
- `lib/picker.sh` — new `_picker_group_min` (assignment-path min table:
  `stripe`/`independent`≥1, `mirror`≥2, `raidz1`≥3, `raidz2`≥4,
  `none`/`single`=1); `_picker_validate_group` repointed at it
  (`picker_validate_layout` left untouched). New pure slicer
  `picker_build_assignment <profile> <disk...>` (+ `_picker_slice_json`):
  slices per `disk_count` in declared order, wrong-total aborts naming the
  expected count, `os_pool none`/`single` defaults to 1.
- `lib/config/profile.sh` — schema gains `os_pool.disk_count`,
  `storage_groups[].disk_count`, `data_pools[].disk_count`. No accessors
  (slicer reads the profile JSON directly; drift guard is one-directional).
- `install.sh` — `_install_pick_assignment` multi branch now slices via
  `picker_build_assignment` + renders the per-group mapping to stderr
  (`_install_render_assignment`); single branch unchanged.
- `vm/lib/profile.sh` — `_profile_resolve_host` multi branch slices the
  VM's `/dev/sdX` list via the same producer; aborts on count mismatch.
- Data: `disk_count` added to `arch-data` (os_pool 1, tank0 1, tank1 2),
  `desktop` (os_pool 2, `data` 3), `arch-secure` (os_pool 2). The one
  `host_profile`-multi VM profile (`vm/profiles/headless/secure.jsonc`)
  already has 2 disks = arch-secure sum ✓.
- New `tests/vm/profiles/data-pools/from-profile.jsonc` —
  top-level `host_profile: arch-data`, 4 disks. `profile_resolve_config`
  produces sda→rpool, sdb→tank0, sdc+sdd→tank1, owned by vm-data
  (verified pure, matches the hand-written inline `plain.jsonc`).

**Decision (operator-confirmed):** assignment-path raidz mins follow the
issue AC (`raidz1`≥3 / `raidz2`≥4), which DIVERGES from
`_zfs_validate_pool_topology` (`raidz1`≥2 / `raidz2`≥3, used by
`layout_validate` for data pools). Stricter at the producing side = safe;
reconciling the two validators is a possible later cleanup.

**AC5 / issue 08 AC3 — VERIFIED on the live VM (2026-06-11).**
`vm.sh --testing --verify-boot --recreate --profile data-pools/from-profile`
ran green end-to-end on the eterniox libvirt host:
- Producer: the install log shows the slicer's exact per-group mapping —
  `rpool` (none) → `/dev/sda2`, `tank0` (stripe) → `/dev/sdb1`,
  `tank1` (mirror) → `mirror /dev/sdc1 /dev/sdd1`.
- Installer reached `===INSTALLER-EXIT-0===`; `vm-data` created.
- Boot-verify booted the installed disk and the pool verifier emitted
  `===FIRSTBOOT-OK===` (emitted ONLY when every check passes,
  `vm-pool-verify.sh`): rpool/tank0/tank1 imported, `tank0/data`→
  `/data/tank0` + `tank1/data`→`/data/tank1` mounted, both /data pools
  owned + writable by `vm-data`, every leaf vdev a stable by-id path.

(Note: the run was interrupted once by a host reboot; on restart libvirtd
hit the known eterniox fTPM systemd-CREDENTIALS failure — unrelated to this
work — and was repaired before the clean `--recreate` re-run above.)
